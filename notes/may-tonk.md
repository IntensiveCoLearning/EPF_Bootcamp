---
timezone: UTC+8
---

# Justin Li

**GitHub ID:** may-tonk

**Telegram:** 

## Self-introduction

EPF 实习计划

## Notes

<!-- Content_START -->
# 2026-04-21
<!-- DAILY_CHECKIN_2026-04-21_START -->
## 结合我前面学过的 EL / CL、交易、MPT 与 Verkle Tree 的阶段性总结

今天我继续学习了 `EPF wiki` 里的 `research/roadmap`。这次和前几次不一样，我不再把它当成一页单独的“路线图介绍”来看，而是把它放回我前面已经学过的知识体系里：**交易是怎么进入链的、节点怎么执行、状态怎么存、区块怎么验证、为什么还要继续升级协议。** 这样一来，我会发现，Roadmap 讲的并不是几个孤立的名词，而是 Ethereum 为了长期生存、扩容、安全和去中心化，正在同时推进的一整套方向。与此同时，我也意识到，EPF 的这页更像一个“研究导航页”，而不是最完整、最新的官方总表；因为 ETH Protocol Fellows 仓库里对应的 issue 明确写到，这个 wiki 的 roadmap 表格当时只完成了大约一半，比较完整的是 `Merge`、`Surge` 和 `Scourge (MEV track)`，而 `Verge`、`Purge`、`Splurge` 等部分是缺失的。相比之下，[ethereum.org](http://ethereum.org) 的新版 roadmap 现在已经把路线图按“更便宜的交易、更强的安全性、更好的用户体验、面向未来”重新整理过了。

在真正理解这页之后，我首先纠正了一个很重要的误区：**Roadmap 不是一个死板的时间表，而是一张问题地图。** 官方 roadmap 直接写明，Ethereum 的发展是社区驱动的，而且会随着研究进展不断变化；它不是“先干完 A，再干 B，再干 C”的线性项目，而是多个方向并行推进的长期工程。官方总览页现在把总体目标概括得很清楚：让 Ethereum 更可扩展、更安全、更可持续；并且它列出了近几年的主升级节点：Paris（The Merge，2022-09-15）、Shapella（2023-04-12）、Dencun（2024-03-13）、Pectra（2025-05-07）、Fusaka（2025-12-03）已经上线，而 Glamsterdam（H1 2026）和 Hegotá（H2 2026）仍在开发中。对我来说，这意味着我在读 `Merge / Surge / Verge / Purge / Splurge` 这些词时，不能把它们当成一次性“完成”的标签，而应该把它们理解成 Ethereum 长期要解决的几类核心难题。

如果让我用我现在的理解，把整个 Roadmap 浓缩成一句话，那就是：**Ethereum 正在同时解决四件大事——让交易更便宜、让协议更安全、让验证和跑节点更轻、让用户体验和长期可维护性更好。** 这四件事，其实正好和我前面学过的很多内容一一对应。比如我前面学过 JSON-RPC 和交易，就更容易理解“更便宜的交易”和“更好的用户体验”；我学过 EL / CL 和 Engine API，就更容易理解“更安全的共识和出块机制”；我学过 MPT 到 Verkle Tree，就更容易理解“更轻的节点验证和更低的状态负担”。所以我现在再看 Roadmap，看到的已经不是一些抽象的名字，而是“Ethereum 整体架构在往什么方向演化”。

* * *

## 我对 `Merge` 的理解：不是“已经结束”，而是“PoS 底座已经建立”

过去我一看到 `Merge`，第一反应是“这不就是已经完成了吗”。但现在我明白，`Merge` 不能只理解成一次历史事件，而应该理解成 **Ethereum 彻底切到 PoS 这条线的起点**。官方路线页写得很明确，Paris（The Merge）在 2022 年 9 月 15 日上线，它完成了从高能耗 PoW 到 staking-based PoS 的切换，把 Beacon Chain 和主网正式合并，还把能耗降低了约 99.95%。对我来说，这意味着 Ethereum 的共识底座已经换了，之后很多改进都建立在这个基础上。

但我现在不会再说“Merge 做完了，所以这条线结束了”。因为官方 security 页面继续在讲 PoS 之后还要处理的安全问题：如何让最终确定更快、如何防攻击、如何防审查、如何保护验证者。它特别强调，即使 Ethereum 已经非常安全，也还要继续增强它面对未来各种攻击的韧性。Shapella 只是把质押提款开放出来，让验证者可以提取质押和奖励；可真正更长期的工作，还包括更快的 finality、更强的抗审查性、更合理的 proposer 和 builder 分工等。换句话说，在我现在的理解里，`Merge` 不只是“PoW→PoS”四个字，而是 **PoS 时代整个安全架构的起点**。

* * *

## 我对 `Surge` 的理解：扩容主线，而且核心不是把 L1 变成单体高速链

如果说哪一条路线最容易让我建立直觉，那一定是 `Surge`。因为它最直接地回答了一个用户层问题：**为什么 Ethereum 交易贵，怎么才能让它便宜下来。** 现在我明白了，Ethereum 的扩容主线并不是“把 L1 直接堆到无限 TPS”，而是让 **L2 / rollups 成为主要交易执行层，而 L1 持续提供结算、安全和数据可用性**。官方 scaling 页面明确说，Ethereum 主要是通过 layer 2 / rollups 扩容的，rollups 会把交易在链下批量处理后，再把结果发回 Ethereum；问题在于，它们当前使用数据的方式还太贵，所以要继续优化。

这时候我就会把 `Dencun`、`proto-danksharding`、`blob` 和 `PeerDAS` 都放到 `Surge` 这条主线上去理解。官方总览页说，Dencun 上线了 EIP-4844，也就是 proto-danksharding，引入了 blob transactions，用更便宜、临时的数据存储方式显著降低 rollup 成本。Pectra 又把 blob 目标数量提高了，Fusaka 则进一步引入了 Peer-to-Peer Data Availability Sampling（PeerDAS）和更灵活的 blob 参数调整机制。这样一来，我就能把 `Surge` 理解成：**不是简单“让主网更快”，而是持续把 Ethereum 变成一个更适合承载 L2 的安全数据层和结算层。** 这也让我明白，为什么今天官方 scaling 页会把“更便宜的交易”几乎完全放在 rollup 和数据可用性上来讲，而不是只盯着 L1 本身。

* * *

## 我对 `Scourge` 的理解：这条线不是扩容，而是反中心化、抗审查和反不公平提取

一开始我觉得 `Scourge` 最抽象，因为它不像 `Surge` 那样一眼看出是扩容，也不像 `Verge` 那样一眼看出是 Verkle。现在我慢慢理解了：`Scourge` **核心不是让 Ethereum 更快，而是让它在现实博弈里不那么容易被少数人控制。** 虽然当前官方路线页不再把 `Scourge` 当成一级标签单独展示，但它对应的问题，在 security 页面里其实非常明显：防攻击、防审查、保护验证者、让区块提议和构建不被极端集中。

这里我最容易抓住的关键词是 **PBS（proposer-builder separation）**。官方总览页已经把 Glamsterdam 的一个主要特性列成了 **enshrined proposer-builder separation**，意思是把“区块提议”和“区块构建”更原生、更安全地拆开，让验证者不必自己做所有组装工作，也不需要盲目信任外部软件。对我来说，这条线的意义是：Ethereum 不只是追求“能跑”，还在努力让出块排序和价值提取的权力不被少数参与者绑架。再结合 security 页里关于抗审查、保护验证者的叙述，我现在会把 `Scourge` 理解成一句话：**让 Ethereum 不只是性能和安全强，还要在利益博弈中尽量保持公平、开放和抗审查。**

* * *

## 我对 `Verge` 的理解：它和我今天学的 MPT → Verkle Tree 是最直接连上的

在所有路线里，`Verge` 是我最容易和前面知识连起来的一条。因为我今天刚学过 **MPT 到 Verkle Tree**，已经知道 Ethereum 当前用 MPT 组织状态、交易和 receipts 的承诺，而 Verkle Tree 则是为了更小的 witness、更轻的验证、更适合 stateless client 的未来结构。现在再看官方 statelessness 页面，我就会觉得很多话一下子变得有意义了。页面写得非常直接：**想让更多人能在普通硬件上跑节点，关键是降低对大状态数据库的依赖；现在最大的门槛是磁盘空间，而不是单纯的计算能力。** 页面甚至明确写到，当下运行全节点推荐使用快速的 2TB SSD。

这就让我真正明白了 `Verge` 的目标不是“换一棵树”这么表层的事，而是：**让验证新区块不再强依赖我本地保存完整状态。** 如果未来状态证明足够小，节点就可以依赖 witness 来验证块，而不必每个人都一直背着一整份不断膨胀的全局状态库跑。也就是说，`Verkle Tree` 在我的知识图谱里，不再只是某种新数据结构，而是轻节点、statelessness、可持续去中心化的基础设施。这样一来，`Verge` 这条线和我前面学的 `stateRoot`、MPT、storage trie、proof/witness 就真正接起来了。

* * *

## 我对 `Purge` 的理解：不是简单删数据，而是减轻历史、状态和协议负担

一开始我会直觉把 `Purge` 理解成“把旧数据删掉”。但现在我觉得这个理解太粗糙了。更准确地说，`Purge` 处理的是两个大问题：**一是节点为什么越来越重，二是协议为什么越来越复杂。** 官方 statelessness 页面提到 `state expiry` 和 `history expiry`，其实就是在正面面对一个现实：如果每个节点都默认长期保存所有历史和全部状态，Ethereum 会越来越难跑、越来越不利于去中心化。

与此同时，官方 future-proofing 页面也让我意识到，`Purge` 不只是“减存储”，也是 **减技术债**。那一页明确说，有些路线并不是为了短期扩容或短期安全，而是为了让 Ethereum 在几十年尺度上仍然保持稳定、可靠。它列出量子抗性、协议简化和更高效的长期结构作为重点。这让我理解到，`Purge` 不只是“旧历史别背着了”，而是 **把不必要的复杂性和过重的历史包袱，从普通节点和核心协议里尽量卸下来。** 这样，Ethereum 才能既轻一点，也更容易长期维护。

* * *

## 我对 `Splurge` 的理解：它不是“边角料”，而是很多长期关键改进的集合

如果我以前看到 `Splurge`，可能会下意识以为这就是“其他没地方放的杂项”。但现在结合官方 user experience 和 future-proofing 页面，我会觉得这种理解太轻视它了。因为很多真正影响用户采用和长期可持续性的改进，都不一定能严格归类到“扩容”或“共识安全”里。官方 user-experience 页面就写得很清楚：Ethereum 目前对大多数人来说仍然太复杂了，要实现大规模采用，必须大幅降低使用门槛，让用户既能享受 permissionless 和 censorship-resistant 的好处，又能拥有接近 Web2 的顺滑体验。([ethereum.org](http://ethereum.org))

这让我马上联想到 account abstraction、智能合约钱包、seed phrase 之外的更好账户体验。官方总览页也把 Pectra 的一个主要特性写成“让 EOA 钱包具备智能合约功能”，从而支持批量交易、gas sponsor、恢复机制等。这些都不像 blob、Verkle 那样“协议味很重”，但它们对普通用户来说可能反而更能直接感知。再加上 future-proofing 页里提到的量子抗性和协议简化，我现在会把 `Splurge` 理解成：**那些不一定最耀眼，却深刻影响 Ethereum 是否真的适合普通人使用、是否能稳定活很多年的关键工程。**
<!-- DAILY_CHECKIN_2026-04-21_END -->

# 2026-04-20
<!-- DAILY_CHECKIN_2026-04-20_START -->

许多区块链共识协议是存在分叉（forkful）的，网络中的节点由于网络延迟、中断、消息乱序或恶意行为，可能会对网络状态产生不同的视图。**叉选机制（Fork-choice Mechanism）的作用正是帮助所有诚实节点达成一致，形成一段相同的、线性的历史记录和系统状态视图**。

以下是叉选机制的核心运作原理及在以太坊中的具体应用：

**1\. 核心运作原理**

-   **选择头区块与打分**：叉选规则会根据节点的本地视图，为区块分配一个数字分数。得分最高者获胜并成为“头区块”（head block），通过选择这个位于分支顶端的头区块，节点可以隐式地确定一条追溯到创世区块的唯一线性区块链，即最有可能成为最终规范链（canonical chain）的分支。
    
-   **有效性前提**：任何叉选的首要规则是，所选的区块及其所有祖先区块都必须符合协议的有效性规则。任何无效区块都会被直接忽略，且基于无效区块构建的子区块同样被视为无效。
    

**2\. 典型的叉选规则** 不同的共识机制采用了不同的叉选规则：

-   **工作量证明（PoW）**：比特币和早期的以太坊使用的是\*\*“最重链规则”\*\*（heaviest chain rule，常被不太准确地称为“最长链”），即选择累积“工作量”最多的链作为头区块。值得一提的是，尽管早期有传言，但以太坊在 PoW 阶段由于担忧某些未指明的攻击，实际上并未真正实现过 GHOST 协议，而是依赖更简单、经过充分测试的最重链规则。
    
-   **Casper FFG（权益证明）**：以太坊的 PoS 协议之一，其叉选规则是\*\*“跟随包含最大高度（greatest height）的已证明检查点（justified checkpoint）的链”\*\*，并且绝对不允许回滚已经最终确定（finalized）的区块。
    
-   **LMD GHOST（权益证明）**：这也是以太坊 PoS 协议的一部分，采用\*\*“贪婪最重观察子树”\*\*（Greediest Heaviest Observed SubTree）规则。它通过计算验证者对区块及其子代区块的累积投票数来决定链的走向，同时也适用 Casper FFG 的规则。
    

**3\. 重组（Reorgs）与回滚（Reversion）机制** 当节点接收到新区块或新的验证者投票时，它会利用这些新信息重新评估叉选规则。大多数情况下，新区块是当前头区块的子区块；但有时新区块可能位于另一条不同的分支上。

-   **重组过程**：当叉选规则判定新头区块位于不同分支时，节点必须进行“重组”（reorg）。这意味着节点会**回滚（撤销）先前已经包含在链上的区块，并采用新头区块所在分支上的区块**。
    
-   **具体示例**：假设节点当前的链是 `A ← B ← D ← E ← F`（此时认为 F 是最佳头区块），并且节点知道有一条包含了区块 C 的侧链。如果随后节点收到建立在 C 之上的新区块 G，且叉选规则认为 G 是更好的头区块，那么节点就会回滚 D、E 和 F 这三个区块（就如同从未收到过一样），退回到区块 B 的状态。接着，节点将 C 和 G 加入链中，使规范链变为 `A ← B ← C ← G`。
    
-   **重组频率**：由于网络延迟，发生包含一两个区块的短暂重组是非常常见的现象。但较长深度的重组应该极为罕见，除非网络正遭到攻击，或是叉选规则及代码实现中存在漏洞。
    

**4\. 在以太坊中的综合应用（Gasper）** 以太坊的共识协议将 LMD GHOST 和 Casper FFG 组合在一起，统称为 **"Gasper"**。

-   **LMD GHOST 优先保证活性（Liveness）**，确保系统能够持续生成新区块并向前推进链条。
    
-   **Casper FFG 负责提供安全性（Safety）**，通过定期最终确定区块，保护区块链免受长时间的深度回滚攻击。 在网络状况不佳或出现网络分区（Network partition）时，**以太坊的设计原则是优先保证活性**。这意味着即使 Casper FFG 暂时无法达成一致并最终确定区块，LMD GHOST 仍会确保网络继续处理交易和生成区块，优先让链保持运转。
<!-- DAILY_CHECKIN_2026-04-20_END -->

# 2026-04-19
<!-- DAILY_CHECKIN_2026-04-19_START -->


欠一天，最近有好多事，要详细安排一下计划，一边在研究链上数据分析，一边参加竞赛，一边准备期中考，一边学习EIP，一边准备学习chainlink的CRF，好多呀，要详细调整一下计划和状态了；所以今天先欠一天；明天补上
<!-- DAILY_CHECKIN_2026-04-19_END -->

# 2026-04-18
<!-- DAILY_CHECKIN_2026-04-18_START -->



# 以太坊 PoS 中退出验证者作恶问题的完整总结

## —— 为什么他们还能作恶，以及系统究竟如何解决

当我深入学习以太坊从 PoW 转向 PoS 之后，我逐渐意识到一个非常容易让人困惑的问题：既然验证者已经退出系统了，为什么他们还能对链造成威胁？如果退出的人已经不再参与共识、不再拥有未来的投票权，那他们理论上不是应该彻底失去影响力吗？

这个问题表面上看很矛盾，但它恰恰触及了 PoS 安全模型最核心的设计逻辑。真正的答案并不是“退出验证者仍然控制链”，而是：**退出验证者虽然无法影响未来，但在某些条件下，他们可能被利用来伪造过去。**

这也是以太坊为什么引入 Weak Subjectivity（弱主观性）机制的根本原因。

* * *

## 一、退出验证者到底还能做什么？

首先必须明确一点：退出验证者不能直接修改当前主链，也不能重新获得未来的出块权。他们失去的是未来参与共识的资格，例如：

-   未来 slot 的区块提议权
    
-   未来 attestation 投票权
    
-   后续 staking 奖励资格
    

也就是说，他们已经不能再影响当前在线运行的网络。

但是，他们通常仍然保留着一件东西：**验证者私钥**。

这意味着，虽然他们不能投票未来区块，但他们依然可能使用旧私钥，对过去某个历史时间点重新签名，从而配合攻击者构造一条“替代历史链”。

所以问题的本质不是：

> 退出验证者还能控制未来吗？

而是：

> 退出验证者是否还能被利用来伪造过去？

答案是：在理论上可以。

* * *

## 二、什么叫“伪造历史链”？

很多人会误解成攻击者直接修改链上的数据库，把旧区块替换掉。实际上并不是这样。

真实情况更像是：

假设当前真实链是：

```
A → B → C → D → E
```

攻击者利用旧验证者私钥，从过去某个点重新构造另一条链：

```
A → B → X → Y → Z
```

然后为 X、Y、Z 附上大量旧验证者签名，让这条链看起来像是“当时获得过共识支持”。

对于一直在线的节点来说，这种攻击没有意义，因为它们早已亲历真实历史，知道 C、D、E 才是真实延续。

但对于刚上线的新节点来说，它没有历史上下文，只看到两条都“看起来合法”的链，于是可能被误导。

这就是所谓的 **Long-Range Attack（远程历史攻击）** 或替代历史攻击。

* * *

## 三、为什么新节点不能自己判断真假？

这也是最关键的问题之一。

很多人会问：

> 新节点为什么不能去问老节点？  
> 为什么不能看区块时间？  
> 为什么不能看哪条 finalized 更多？

这些直觉看似合理，但在去中心化网络里并不充分。

* * *

### 1\. 不能单纯相信网络里的节点

因为新节点刚加入网络时，它连接到的是一批陌生 peer。  
其中可能有诚实节点，也可能有恶意节点。

新节点无法天然知道：

-   哪个节点是真正长期在线的老节点
    
-   哪个节点是攻击者伪装的节点
    

也就是说，**网络本身不是可信来源**。

* * *

### 2\. 不能靠区块时间判断

区块链里的时间并不是现实世界绝对时间，而是协议时间（slot、epoch、timestamp）。

攻击者虽然是在未来伪造历史链，但完全可以把区块内容写成过去对应的 slot 序列，例如：

```
slot 50000
slot 50001
slot 50002
```

所以新节点看到的是“链内声明的时间”，而不是攻击者现实中生成区块的时间。

* * *

### 3\. Finality 也不能单独解决问题

Finality 的作用是：

> 如果你已经在一条链上，它能保证这条链的近期历史不会轻易回滚。

但 Finality 不能保证：

> 你一开始选中的那条链就是真链。

攻击者构造的替代历史链，也可能伪装出 finalized 结构，让新节点难以区分。

* * *

## 四、既然有这些问题，以太坊是如何解决的？

这里必须理解一个关键事实：

**以太坊并不是通过“彻底禁止退出验证者作恶”来解决问题，而是通过多层机制，让这种作恶变得无效、低收益、难传播。**

这是一种典型的安全工程思维。

* * *

# 第一层：Weak Subjectivity（弱主观性）——最核心方案

这是解决 Long-Range Attack 的核心机制。

新节点在同步时，不要求从创世区块开始无条件相信网络，而是要求先获得一个可信的 checkpoint（检查点）。

例如：

```
某 slot 的 finalized block hash
```

然后节点从这个检查点开始同步。

这意味着攻击者即使伪造从创世开始的历史链，也无法影响已经拥有 checkpoint 的节点，因为节点不会从零开始相信陌生链。

这就是所谓：

> 给新节点一个可信起点。

* * *

# 第二层：Finality（最终性）

Casper FFG 机制让链上 checkpoint 在获得 2/3 stake 投票后 finalized。

作用是：

-   锁定近期历史
    
-   降低链回滚概率
    
-   保证在线节点的稳定性
    

因此退出验证者即使签历史链，也难以撼动当前在线网络已经 finalized 的真实链。

* * *

# 第三层：退出延迟 + 提款延迟

验证者退出并不是按下按钮立即离开。

实际流程是：

```
申请退出
→ 排队等待（exit queue）
→ 正式退出
→ 再等待可提款
```

这段延迟非常重要，因为它给系统留出了观察窗口。

如果验证者在退出前后尝试双签、作恶，仍可能被 slash（罚没）。

也就是说：

> 退出不是瞬间免罪卡。

* * *

# 第四层：Slashing（罚没机制）

对于仍处于系统中的验证者，如果发生：

-   双重投票
    
-   surround vote
    
-   同时支持冲突链
    

都会受到罚没处罚。

虽然 slashing 无法完全约束已经彻底退出且提币完成的人，但它能极大降低验证者在退出前后的作恶动机。

* * *

# 第五层：社会共识（Social Consensus）

这是很多技术讨论里被忽略，却现实中非常重要的一层。

如果真的出现明显替代历史攻击，整个生态系统会共同识别真实链，包括：

-   客户端开发团队
    
-   Staking 服务商
    
-   节点运营者
    
-   基础设施提供商
    
-   用户与交易所
    

大家会一致承认哪条链是合法延续链。

这也是为什么它叫：

```
Weak Subjectivity
```

因为最终仍然需要极少量社会层信任。

* * *

## 五、为什么说攻击“还能做，但没意义”？

退出验证者确实可能保留旧私钥，也确实可以签历史消息。

但真正决定安全性的不是“能不能签”，而是：

```
签出来有没有人认
```

现实中：

-   在线节点不会认
    
-   有 checkpoint 的新节点不会认
    
-   客户端默认链不会认
    
-   交易所不会认
    
-   社区不会认
    

于是攻击收益极低。

所以以太坊的设计逻辑不是：

> 物理上禁止作恶

而是：

> 即使作恶，也无法获利、无法扩散、无法成为主流链。

* * *

## 六、最终总结

退出验证者的问题，并不是他们还能控制未来，而是他们理论上还能被利用来伪造过去。因为 PoS 的共识权来自签名，而私钥在现实世界中无法被链上系统自动销毁，所以历史签名能力并不会瞬间消失。

但以太坊通过 Weak Subjectivity、Finality、退出延迟、Slashing 以及社会共识等多层机制，成功把这种攻击限制在理论层面，使其几乎无法产生实际收益。
<!-- DAILY_CHECKIN_2026-04-18_END -->

# 2026-04-17
<!-- DAILY_CHECKIN_2026-04-17_START -->




关于ssz的一个补充

**SSZ 是如何进行序列化的？**

SSZ 将以太坊中的数据类型分为基本类型和复合类型，并分别制定了严格的序列化规则：

**1\. 基本类型（Basic Types）**

-   **无符号整数（uintN）**：SSZ 支持 8 到 256 位的无符号整数。整数会被直接转换为\*\*小端序（Little-Endian）\*\*的字节数组表示。例如，`uint16` 类型的十进制数 1025（十六进制为 `0x0401`），由于低位字节排在前面，会被序列化为 `01 04`。
    
-   **布尔值（Booleans）**：使用单字节表示。`True` 序列化为 `01`，`False` 序列化为 `00`。
    

**2\. 复合类型（Composite Types）**

-   **向量（Vectors）**：用于存储**固定长度**的同构元素。SSZ 会将向量中的每个元素独立序列化后直接拼接在一起。由于长度和数据类型是固定的，因此不需要在序列化数据中包含长度前缀等元数据。
    
-   **列表（Lists）**：用于存储具有最大容量限制的**可变长度**同构元素。序列化时，所有元素独立序列化并拼接。根据不同的实现，可能需要在数据开头包含长度元数据；如果是被嵌套在其他对象中，处理方式与固定长度的向量不同，会带来少量额外开销。
    
-   **位向量（Bitvectors）与位列表（Bitlists）**：用于高效紧凑地存储布尔值（如权限、状态标志位）。位向量是固定长度的，所有位（`0`代表False，`1`代表True）会被打包进字节中，每个字节从最低有效位（LSB）向最高有效位（MSB）排列，不足 8 位的末尾补零。位列表则是可变长度的，除了上述的打包规则外，它必须在位序列的最后**添加一个值为 1 的“哨兵位（Sentinel Bit）”**，以让反序列化过程明确知道该位列表的实际结束位置。
    
-   **容器（Containers）**：容器类似于编程语言中的对象或结构体，用于将多个不同字段（甚至嵌套其他列表或容器）合并成一个复合类型。在序列化复杂的容器（如信标链的 `IndexedAttestation`）时，**固定大小的字段会被直接按顺序放入字节流**；而对于列表等可变大小的字段，则会在原本的位置放入一个**4字节的偏移量（Offset）**，指示该可变数据的实际存储起始位置，而真正的可变数据会被统一拼接在整个容器的尾部
    

**Beacon-API** 是针对以太坊信标节点（Beacon Node）制定的 API 规范，它的核心作用是允许用户查询并参与到以太坊 2.0（信标链）网络中。

以下是关于 Beacon-API 的详细总结：

**1\. 数据格式与通信标准**

-   **默认使用 JSON**：默认情况下，所有的请求和响应都采用 JSON 格式。因此，客户端的请求头通常需要包含 `"Content-Type: application/json"` 或 `"Accept: application/json"`。
    
-   **支持 SSZ 格式**：为了提高效率，部分特定的请求也支持发送和接收 **SSZ（简单序列化）**格式的数据。若要使用 SSZ 格式，请求头需要设置为 `"Content-Type: application/octet-stream"`。
    
-   **严格的状态码响应**：当处理请求时，如果客户端在 `Content-Type` 中指定了服务器不支持的格式，服务器必须返回 **415 状态码**。同样地，如果服务器无法提供客户端在 `Accept` 头部所要求的数据格式（若未提供，则默认假设为 application/json），服务器必须返回 **406 状态码**。
    

**2\. API 版本控制机制**

-   **独立版本号**：API 的端点（Endpoints）是**独立进行版本控制**的。因此，所有的 v1 端点或所有的 v2 端点之间并没有直接的关联，不能强行推断它们之间的对应关系。
    
-   **响应数据结构**：在所有的 JSON 响应中，请求的核心数据都会被统一放置在响应最顶层的 `data` 键下。
    
-   **版本号递增规则**：API 的版本号变更非常严格。只有当某个端点**移除了现有字段**，或者**改变了某个字段的格式**（例如把字符串变成了数组）时，才必须增加版本号。相反，如果仅仅是在端点的数据或元数据中**新增字段**，则不需要增加版本号。
    

**3\. 核心功能端点分类** Beacon-API 将功能划分为了多个不同的集合，以满足各种查询和操作需求：

-   **Beacon（信标链查询）**：这是最核心的查询端点集合，用于获取信标链的数据。包括查询创世区块详情（genesis）、获取区块和区块头信息（blocks/headers）、获取状态信息（states，其中包括最终确定性检查点、验证者列表及余额、委员会分布等），以及获取 Blob sidecars 等。
    
-   **Pool（操作内存池）**：用于获取或向节点的本地池中提交各类网络操作。例如提交和查询证明（attestations）、证明者和提议者的罚没（slashings）、自愿退出（voluntary exits）以及 BLS 到执行层的变更签名等。
    
-   **Light Client（轻客户端支持）**：专门为以太坊轻客户端提供运行所需的数据结构，包括获取引导程序（bootstrap）、同步委员会周期的更新、最新的最终确定性更新（finality\_update）以及乐观更新（optimistic\_update）。
    
-   **Validator（验证者操作）**：专为验证者客户端设计的端点。其中，`ValidatorRequiredApi` 定义了一个验证者正常运作所必需的**最小端点集合**。
    
-   **Rewards（奖励与惩罚）**：用于查询验证者在网络中的表现，包括获取同步委员会奖励、区块奖励以及证明奖励。
    
-   **Config（网络配置）**：用于查询当前链的配置参数、规范细节以及未来的分叉时间表。
    
-   **Node & Events（节点状态与事件）**：提供查询节点本身相关信息的端点，并支持通过 Events 端点进行事件订阅。
    
-   **Debug（调试）**：提供用于调试链状态的端点。需要注意的是，**这些端点不应公开暴露**给外部网络
    

Eth Beacon Node API 本质上是共识层对外暴露的控制接口，它通过标准化的 RESTful 设计，将复杂的共识协议封装成稳定、可版本化、可扩展的 HTTP 服务，使开发者无需接触底层 P2P 和共识算法，就可以获取链状态、执行验证者行为、监听实时事件，并构建监控系统、自动化策略和数据分析工具。它在整个以太坊架构中承担的是“外部控制面”的角色，与内部的 DevP2P（节点通信）和 Engine API（CL↔EL 协作）形成清晰分层，从而实现模块化、解耦和生态扩展能力
<!-- DAILY_CHECKIN_2026-04-17_END -->

# 2026-04-16
<!-- DAILY_CHECKIN_2026-04-16_START -->





## 第一阶段：发现与身份确认（建立通讯录）

在茫茫网海中，我首先需要找到那些诚实的节点并确认他们的“身份卡”。

-   **找人 (discv5)**：我利用基于 UDP 的 **discv5** 协议在网络中动态搜索其他对等节点 。它像是一个分布式搜索引擎，帮我发现活跃的节点 。
    
-   **身份卡 (ENR)**：我通过读取 **ENR (Ethereum Node Records)** 来识别节点 。这上面记录了节点的公钥、IP、端口以及它支持的协议 。每张卡都有签名和序列号，确保我拿到的信息是最新且未被篡改的 。
    
-   **永久 ID (Peer ID)**：我关注每个节点唯一的 **Peer ID** 。即便它的物理地址变了，只要身份哈希没变，我依然能通过 **Multiaddress** 这种灵活的地址格式精准定位它 。
    

* * *

## 第二阶段：连接与安全（建立加密通道）

在确认对方身份后，我需要建立一条安全、高速的专线。

-   **传输协议 (libp2p)**：我建立物理连接时依赖 **libp2p** 。我优先选择 **QUIC** 以获得极速的握手体验和内置的多路复用能力，同时保留 **TCP** 作为最可靠的保底方案 。
    
-   **加密通话 (libp2p-noise)**：为了防止被中间人窃听，我强制开启 **libp2p-noise** 加密 。我采用 **Noise XX 握手模式**，让双方在传输数据前完成双向认证和密钥交换 。
    
-   **并发处理 (Multiplexing)**：我使用 **Yamux**（默认）或 **mplex** 多路复用器 。这让我能在同一个加密通道里并行处理多个逻辑流，比如一边检查节点存活，一边同步区块数据 。
    

* * *

## 第三阶段：编码与通讯（说标准语言）

为了确保全球成千上万个节点能够“无障碍交流”，我必须使用极其严谨的语言。

-   **官方语言 (SSZ)**：我全面采用 **SSZ (Simple Serialize)** 替代了旧的 RLP 编码 。它的**确定性**非常强，同样的区块在任何节点上序列化后的字节序列都完全一致 。
    
-   **内容寻址与验证**：我利用 SSZ 的**默克尔化 (Merkleization)** 特性，将复杂数据组织成树状结构 。这让我只需一个根哈希（Root）和少量的路径证明，就能验证我硬盘里存的内容是否真实可靠，而无需读取整个庞大的数据库 。
    
-   **消息传递模式**：
    
    -   **Gossipsub**：我通过主题（Topics）向全网高效广播新区块和验证者证明，这种网格优化避免了带宽浪费 。
        
    -   **Req/Resp**：当我想向特定邻居拉取某段缺失的历史记录时，我会发起精准的 1:1 请求 。
        

* * *

## 第四阶段：共识与决策（达成真理）

这是我作为共识层节点的核心使命——决定哪条链才是“唯一的真相”。

-   **共识大脑 (Gasper)**：我运行 **Gasper** 机制，它是我达成共识的指挥官 。
    
    -   **LMD GHOST**：它负责选出当前的“头块”，通过统计验证者的投票权重，引导我选择那条“最重”的子树路径，从而处理分叉 。
        
    -   **Casper FFG**：它负责终结历史，每隔一个 Epoch 就给区块盖上“不可撤销”的公章，确保安全性 。
        
-   **状态转换**：我通过执行纯函数 $S' \\equiv f(S, B)$ 来更新我的视图 。只要我从初始状态开始按顺序应用这些 SSZ 序列化的区块，我就能确信我的账本和全球其他诚实节点完全同步 。
    

* * *

**我的总结：**

我现在对 CL 的理解已经从零散的协议变成了一个有机的生命体。我先通过 **discv5** 找到 **ENR** 身份卡，随后利用 **libp2p-noise** 在 **QUIC** 或 **TCP** 上开启加密通话。在 **Yamux** 提供的并行轨道上，我用 **SSZ** 这种具备“内容指纹”能力的语言进行 **Gossip** 或 **Req/Resp** 交流，最后将这一切汇总给 **Gasper** 大脑，完成状态转换并维护以太坊的全球一致性。
<!-- DAILY_CHECKIN_2026-04-16_END -->

# 2026-04-15
<!-- DAILY_CHECKIN_2026-04-15_START -->






以太坊节点记录（ENR，Ethereum Node Records）\*\*提供了一种结构化、灵活的方式，用于在以太坊的点对点（P2P）网络中存储和共享节点的身份与连接详细信息。它是以太坊节点的首选网络地址格式，同时也是一种“面向未来”的格式，能够让新节点更轻松地交换身份信息。

ENR 的核心组件主要包含以下三个部分：

1.  **签名（Signature）**：每个记录都会使用一种身份方案（例如 secp256k1）进行签名，以确保该记录的真实性。
    
2.  **序列号（Sequence Number，简称 seq）**：这是一个 64 位的无符号整数，每次更新节点记录时，该数值都会递增，这使得网络中的其他节点能够以此判断并获取最新版本的记录。
    
3.  **键/值对（Key/Value Pairs）**：记录中通过键值对的形式保存了节点的各种连接详细信息。
    

在具体的数据表现和应用上：

-   **文本编码**：在文本形式下，ENR 的 RLP（递归长度前缀）数据会通过 Base64 进行编码，并可以作为一串以 `enr:` 开头的字符串在客户端之间进行共享。
    
-   **动态更新**：以太坊的发现协议第5版（discv5）允许节点动态地交换和更新彼此的 ENR，从而确保网络中对等节点发现列表的实时性。
    
-   **执行层与共识层共享**：在客户端架构中，执行层客户端（EL）和共识层客户端（CL）会共享同一个 ENR，但在记录中会为每个客户端分别保留独立的密钥（即 eth1 密钥和 eth2 密钥）
    

在以太坊网络中，节点通过 discv5（Discovery Version 5）协议发现彼此的过程主要依赖于以下机制：

-   **独立的底层协议**：discv5 是一个专门用于对等节点（peer）发现的协议，它直接运行在 UDP 协议之上。
    
-   **动态交换以太坊节点记录（ENR）**：节点通过 discv5 动态地交换和更新彼此的 ENR。ENR 提供了一种结构化、灵活的方式来存储和共享节点在网络中的身份标识与连接的详细信息，使得新节点能够轻松地交换身份数据并建立连接。
    
-   **确保发现列表的实时性**：这种动态交换机制确保了节点能够维持一个最新的对等节点发现列表。当节点的连接信息发生变化时，其 ENR 中的序列号（seq）会递增，这使得网络中的其他节点能够识别并获取该节点最新版本的记录。
    
-   **与核心通信协议栈并行**：discv5 协议的任务仅限于网络最初的“节点发现”环节，它与节点间负责实际数据传输、加密和多路复用的核心点对点协议栈 `libp2p` 是并行运行的。
<!-- DAILY_CHECKIN_2026-04-15_END -->

# 2026-04-14
<!-- DAILY_CHECKIN_2026-04-14_START -->







### 1\. libp2p：模块化的通信基石

**libp2p** 不仅仅是一个协议，它是一个**多传输协议栈**，负责处理节点间“怎么连”和“怎么传”的问题。

-   **传输层 (Transport)：** 支持多种路径（TCP, QUIC, WebRTC）。目前的趋势是向 **QUIC** 演进，因为它基于 UDP，握手更快且原生支持加密。
    
-   **多地址 (Multiaddress)：** 解决了传统 IP+端口无法识别身份的问题。它将地理位置（IP）和身份（Peer ID）封装成一段类似 `/ip4/192.168.1.1/tcp/9000/p2p/Qm...` 的格式。
    
-   **多路复用 (Multiplexing)：** 允许在一个连接上同时跑多个流（如一边同步区块，一边交换心跳数据），常用组件是 **yamux** 和 **mplex**。
    

* * *

### 2\. 身份与发现：如何找到并识别“邻居”

在 P2P 网络中，找到正确的节点并确认其身份是首要任务。

-   **Peer ID (对等 ID)：** 节点的唯一指纹，基于公钥生成。即使节点的 IP 变了，只要私钥没变，Peer ID 就不变。
    
-   **ENR (以太坊节点记录)：** 节点的“数字名片”。它包含签名、序列号（用于更新）和键值对（IP、端口、协议支持等）。它是以太坊专用的节点格式。
    
-   **discv5 (节点发现 v5)：** \* 运行在 **UDP** 上，与 libp2p 并行。
    
    -   **职责单一：** 只负责“寻找邻居”。它通过分布式哈希表（DHT）让节点在茫茫网海中找到拥有最新 ENR 的其他节点。
        
-   **Identify 协议：** 连接建立后的“自我介绍”，告知对方自己支持哪些功能版本。
    

* * *

### 3\. 消息传播优化：Gossipsub

在成千上万个节点间广播消息（如新区块）时，传统的“全员广播”会导致网络瘫痪。

-   **Gossipsub (发布/订阅模式)：** \* **网格优化 (Mesh)：** 节点只与少数选定的邻居组成网格进行高效传输，而不是 1:1 连接全网。
    
    -   **带宽节省：** 它避免了节点重复接收同一条消息，将复杂度从 $O(n^2)$ 降低到更可持续的水平。
        
    -   **双重模式：** 大规模消息走 **Gossipsub**（广播），特定的请求（如请求缺失的区块）走 **Req/Resp**（点对点直连）。
        

* * *

### 4\. 安全与加密：Noise 框架

以太坊共识层放弃了过时的加密方式，选择了更现代的 **libp2p-noise**。

-   **Noise 框架：** 它不是单一协议，而是一套构建加密协议的工具箱。
    
-   **XX 握手模式：** 采用“双向传输（Transmit-Transmit）”模式。这意味着发起方和响应方都会交换公钥，确保连接建立时双方都经过了身份验证，且数据传输过程全加密。
    

* * *

### 5\. 编码与压缩：SSZ 与 Snappy

这是数据进入网络传输前的最后一道工序，决定了数据的大小和可验证性。

-   **SSZ (简单序列化)：** \* **取代 RLP：** 以太坊 2.0 的标准编码方式。
    
    -   **核心优势：** 它是为 **Merkle 树** 设计的。这意味着你可以对数据的一部分进行数学证明（Merkle Proof），而无需下载整个大数据包。
        
    -   **确定性：** 相同的数据永远生成相同的字节流。
        
-   **Snappy (快速压缩)：** \* **性能优先：** 相比于压缩率，它更看重**速度**。它能在不消耗过多 CPU 的情况下，迅速减小 SSZ 编码后的体积，进一步优化带宽。
    

* * *

### 总结：数据流转的全过程

1.  **找人：** 通过 **discv5** 的 UDP 发现找到邻居的 **ENR**。
    
2.  **建立连接：** 使用 **libp2p** 根据 **Multiaddr** 拨号。
    
3.  **安全握手：** 通过 **Noise (XX)** 建立加密隧道。
    
4.  **确定协议：** 通过 **Identify** 确认双方都支持以太坊共识协议。
    
5.  **数据交互：**
    
    -   **广播：** 用 **Gossipsub** 发布新区块。
        
    -   **处理：** 数据经过 **SSZ** 编码排列，再由 **Snappy** 压缩，最后由 **Multiaddr** 指向的路径发出。
<!-- DAILY_CHECKIN_2026-04-14_END -->

# 2026-04-13
<!-- DAILY_CHECKIN_2026-04-13_START -->








请假一天，调整状态。
<!-- DAILY_CHECKIN_2026-04-13_END -->

# 2026-04-12
<!-- DAILY_CHECKIN_2026-04-12_START -->









以太坊的**共识层（Consensus Layer, CL）架构**主要围绕权益证明（PoS）的实现

**1\. 核心共识协议：Gasper**

以太坊的 PoS 共识机制被称为 **Gasper**，它是将两个独立协议结合的产物：

-   **LMD GHOST（提供活跃度）**：其分叉选择规则是“最贪婪且最重的已观察子树”，通过统计验证者的累积投票来决定主链。它的核心作用是让链不断向前移动，定期产生新区块。
    
-   **Casper FFG（提供安全性/最终确定性）**：其规则是跟随具有最高高度的合理检查点（justified checkpoint），并确保永远不回滚已被最终确定的区块。它负责在后方夯实基础，保护区块链免受长时间回滚的影响。
    

**2\. 安全性（Safety）与活跃度（Liveness）的权衡**

在共识机制中：

-   **安全性**指“不发生坏事”，例如防止双花，确保诚实节点对状态达成一致。
    
-   **活跃度**指“好事最终会发生”，即区块链能不断添加新区块而不会卡死。
    
-   **CAP 定理与以太坊的取舍**：根据 CAP 定理，在网络通信不可靠时，分布式系统无法同时完美保证安全性和活跃度。以太坊在极端网络状况（如网络分区）下，**优先保障活跃度**。这意味着节点会继续生成区块和处理交易，即使 Casper FFG 暂时无法完成最终确定而带来潜在的安全风险；如果分区持续存在，各方甚至可能确认不同的历史。
    

**3\. 分叉选择规则与链重组（Reorgs）**

由于网络延迟或消息乱序，节点可能对网络状态有不同的视图，分叉选择规则用于帮助诚实节点收敛并选出唯一一条规范链。

-   **基础规则**：候选“头区块”及其所有祖先区块都必须是符合协议规则的有效区块，无效区块会被忽略。
    
-   **重组机制（Reorgs）**：当节点接收到新的区块或投票后，会使用 LMD GHOST 和 Casper FFG 规则重新评估区块树。如果得分最高的新头区块位于不同的分支上，节点就会执行**重组**——即回滚之前接受的区块，转而采用新头区块所在分支的区块。
    
-   一两个区块的短重组在网络中很常见，但较长的重组非常罕见，通常只有在网络遭到攻击、或软件存在 Bug 时才会发生。
    

**4\. 客户端双层架构与组件**

每个以太坊节点都并行运行着两个层的软件：**执行层（EL）客户端**（如 Besu, Geth）和**共识层（CL）客户端**（如 Prysm, Teku）。它们通过本地的 RPC 接口 **Engine-API** 进行通信。共识层的核心组件包括：

-   **信标节点（Beacon Node）**：维护共识链（信标链），接收其他对等节点的共识区块和证明，并与本地的执行节点通信。
    
-   **验证者（Validator）**：替代了 PoW 中的矿工。用户质押 32 ETH 即可运行，负责在系统中提议和验证区块。它们仅听从本地信标节点的指令。
    

**5\. 控制流转：非生成者 vs. 区块生成者**

共识层客户端根据其当前角色的不同，控制流有明显的区分：

-   **作为非区块生成者时**：共识层从网络中接收新区块进行预验证，将交易发送给执行层进行执行和验证。确认无误后，共识层将其加入区块链，并向全网广播证明（attestation）。
    
-   **作为区块生成者时**：共识层收到轮值通知后，调用执行层从内存池中打包交易、执行并生成区块哈希。随后，共识客户端将交易和哈希封装进新的信标区块中，并向全网广播，等待其他节点的验证和最终确定。
    

**6\. 纯函数与时隙驱动的状态转换**

-   **状态转换纯函数**：区块链依赖状态转换函数更新视图，即 _S_′≡_f_(_S_,_B_) （S 为旧状态，B 为新区块）。这是一个纯函数，保证了所有正确应用了相同区块的节点最终状态一致。
    
-   **时隙驱动（Slot-driven）**：与 PoW 依靠区块驱动不同，信标链的状态转换是**由时隙驱动**的。无论时隙内是否有区块产生，状态都会随时间推进而更新。
    
-   **三个层级与有效性**：包括每个时隙（Per-slot）、每个区块（Per-block）和每个纪元（Per-epoch）的状态转换。在这些转换中，如果触发了未处理的异常（如列表越界访问）或导致了数据溢出（如 uint64 的上溢/下溢），该转换就会被视为无效。
<!-- DAILY_CHECKIN_2026-04-12_END -->

# 2026-04-11
<!-- DAILY_CHECKIN_2026-04-11_START -->










学习的内容主要围绕**以太坊协议中的共识层（Consensus Layer, CL）架构**及其核心机制展开。以下是核心知识点的详细总结：

**1\. 核心共识机制：Gasper 协议**

以太坊的权益证明（PoS）共识被称为 **Gasper**，它巧妙地结合了两个独立的子协议，以在工程上实现平衡：

-   **LMD GHOST**：侧重于提供**活跃度（Liveness）**。它通过统计验证者的投票数选出主链，确保系统不断产生新区块、保持链向前运行。
    
-   **Casper FFG**：侧重于提供**安全性（Safety）**。它作为检查点机制，通过定期对区块进行最终确认（Finality），防止区块链发生大规模的历史回滚。
    

**2\. 安全性与活跃度的权衡**

-   **CAP 定理限制**：在不可靠的网络环境中，分布式系统无法同时完美保证安全性（不发生坏事）和活跃度（好事总会发生）。
    
-   **以太坊的取舍**：在面临严重的网络故障或分区时，以太坊**优先保证活跃度**。这意味着，即使 Casper FFG 暂时无法完成区块的最终确定，LMD GHOST 也会让网络继续运行、处理用户交易，即使这会带来潜在的链重组安全风险。
    

**3\. 分叉选择规则与链重组（Reorgs）**

-   **分叉选择规则**：由于网络延迟或消息乱序，节点会产生分歧。分叉选择规则通过为候选区块评分，帮助节点选出最有可能成为最终规范链的“头区块（Head block）”。
    
-   **区块链重组（Reorgs）**：当节点接收到新信息重新评估后，如果发现最佳头区块在另一条分支上，就会执行重组——回滚之前的区块，并切换到新头区块所在的分支。由于网络延迟，一两个区块的短暂重组很常见；而长重组则极为罕见，通常是由网络攻击或软件漏洞引发。
    

**4\. 节点与客户端架构**

-   **双层架构**：每个以太坊节点都同时运行着\*\*执行层（EL）**客户端和**共识层（CL）\*\*客户端。两者并行运行，并通过本地的 **Engine-API** 进行通信协同。
    
-   **共识层关键组件**：
    
    -   **信标节点（Beacon Node）**：负责维持共识链、处理区块、以及与其他对等节点通信。
        
    -   **验证者（Validator）**：需要质押 32 ETH，负责在系统中提议和验证区块（代替了传统 PoW 中的矿工）。
        

**5\. 状态转换机制**

-   区块链依赖纯粹的“状态转换函数”来更新节点视图（通过输入旧状态和新区块，计算出新状态）。
    
-   不同于 PoW 机制（由区块驱动），以太坊信标链的状态转换是\*\*由时隙驱动（slot-driven）\*\*的。它定义了每个时隙（per-slot）、每个区块（per-block）和每个纪元（per-epoch）的状态更新规则。任何导致数据溢出或未处理异常的转换都会被视为无效。
<!-- DAILY_CHECKIN_2026-04-11_END -->

# 2026-04-10
<!-- DAILY_CHECKIN_2026-04-10_START -->











**RLP（递归长度前缀序列化）**

**RLP (Recursive Length Prefix)** 是以太坊执行层中用于编码和解析数据的核心序列化协议。它的主要作用是将数据结构或对象（如交易数据和整个区块链状态）转换为字节流，以确保不同语言编写的客户端软件都能以相同的标准格式可靠、高效地读取和传输数据。

-   **设计理念与特点**：RLP 的设计极其极简，它的唯一目的是存储嵌套的字节数组结构，而不试图定义具体的布尔值、浮点数或整数等数据类型，这些数据的具体含义交由以太坊协议本身来解释。这种设计能够保证跨客户端的字节级别完美一致性，这对于区块链操作所需的确定性至关重要。
    
-   **核心编码规则**：
    
    -   **单字节**：如果输入是单字节且值在 `0x00` 到 `0x7F` 之间，则直接进行编码且保持不变。
        
    -   **短字符串（1-55字节）**：前缀为 `0x80` 加上字符串长度，后跟字符串本身。
        
    -   **长字符串（>55字节）**：前缀为 `0xb7` 加上表示长度的字节数，后跟大端格式的字符串长度，最后是字符串内容的字节。
        
    -   **短列表（总负载1-55字节）**：前缀为 `0xc0` 加上列表编码后所有项的总长度，后跟被依次编码的列表项。
        
    -   **长列表（总负载>55字节）**：前缀为 `0xf7` 加上表示长度的字节数，后跟大端格式的负载长度，最后是列表项的内容。
        
    -   **特殊值**：空字符串、Null 和 False 统一编码为单字节 `0x80`；空列表编码为 `0xc0`。
        
-   **解码过程**：解码器通过检查数据的第一个字节（前缀）落在哪个范围（如 `0x00-0x7F`、`0x80-0xB7`、`0xC0-0xF7` 等）来确定后续数据的类型（单字节、字符串还是列表）以及长度，并通过递归的方式对列表内的元素进行解析。
    

**EOF（EVM 对象格式）**

**EOF (EVM Object Format)** 是一种针对以太坊虚拟机（EVM）字节码的可扩展且带有版本号的容器格式。目前的 EVM 字节码是一串无结构的指令序列，而 EOF 引入了“容器”的概念，为字节码带来结构化设计，使其更易于被 EVM 解析和分析，并在部署时会进行一次性的静态验证。

-   **主要目的与核心优势**：
    
    -   **更好的验证**：在智能合约执行前，可以进行更彻底的静态分析和全面验证。
        
    -   **提高执行效率**：通过划分定义明确的代码段（sections），促进 EVM 执行过程的优化。
        
    -   **增强安全性**：强制执行更严格的代码结构规则，从而减少攻击向量。
        
    -   **面向未来**：通过带版本号的容器，为未来 EVM 的进一步升级和添加新功能打下基础。
        
-   **与传统（Legacy）EVM 字节码的区别**：
    
    -   **结构**：传统方式是无结构的字节码，EOF 则是拥有明确定义的区段。
        
    -   **验证**：传统方式仅有有限的运行时验证，EOF 提供全面的预执行验证。
        
    -   **跳转机制**：传统方式使用动态跳转（`JUMP` / `JUMPI`），EOF 则包含了静态跳转。
        
    -   **类型系统**：传统方式无类型概念，EOF 支持函数签名和类型定义。
        
    -   **数据处理**：传统方式将数据与代码混合存放，EOF 引入了独立的数据段将代码与数据隔离。
<!-- DAILY_CHECKIN_2026-04-10_END -->

# 2026-04-09
<!-- DAILY_CHECKIN_2026-04-09_START -->












# Ethereum 执行层网络 —— 一次 DevP2P 的完整理解

当我第一次学习 Ethereum 的时候，我以为节点之间的通信是一件“理所当然”的事情。我以为只要启动一个节点，它就会自动连接到网络、自动同步区块、自动传播交易。但随着我不断深入，我逐渐意识到，这种理解是完全不够的。一个执行层节点并不是天然就属于网络的一部分，它必须通过一整套过程，才能真正成为网络中的成员。

于是，我开始试着用“我”的视角去理解：如果我就是一个刚刚启动的执行层节点，我到底经历了什么，才真正进入 Ethereum 网络？

当我启动节点的那一刻，我其实只是一个孤立的存在。我加载了自己的配置，读取了本地的节点私钥，并生成了对应的公钥。这个公钥就是我的 Node ID，它是我在整个 P2P 网络中的身份标识。从这一刻开始，我“有了身份”，但我仍然是孤独的。我不知道网络里有哪些其他节点，也没有任何连接。

这时候，我意识到，我的第一步并不是同步区块，也不是执行交易，而是一个更基础的问题：我要先找到别人。

于是，我进入了 Discovery 阶段。这是执行层网络的第一层，也是最容易被忽略的一层。这个阶段的目标非常简单，但也非常关键：让我从“谁都不认识”，变成“认识一批节点”。

我不会对全网发出广播，因为那样效率太低，也不现实。相反，我会先去联系一批预先配置好的节点，也就是 Bootnodes。这些节点就像我进入一个陌生环境时，最先能找到的联系人。它们不是中心服务器，但它们能帮我打开第一扇门。

当我联系上一个 bootnode，我不会立刻和它交换区块或交易。我会先做一件很简单的事情：我发一个 Ping，它回一个 Pong。这一步看似简单，但意义重大。它让我确认对方是在线的，是可达的，是一个真实存在的节点。就像我先敲门，确认屋里真的有人。

确认之后，我不会停在这里。我会继续问：“除了你之外，还有谁值得我认识？”于是我发送 FindNode 请求，对方会返回一批它认为“更接近目标”的节点。这时候，我开始逐渐意识到，节点发现不是随机的，而是有结构的。我并不是随便认识人，而是在不断向“更有价值的节点”靠近。

这种查找方式背后，其实就是 Kademlia 的思想。它让我不用扫描整个网络，而是一步一步、沿着更接近目标的方向递归推进。就像问路一样，我不是问全城的人，而是问那些看起来更接近目的地的人。

随着这个过程不断进行，我认识的节点越来越多。我不仅知道它们的存在，还会获取它们的 ENR，也就是它们的“节点名片”。这张名片里包含了它们的 IP、端口、公钥等信息，让我真正知道如何连接它们。

这些信息不会被我随手丢弃。我会把它们整理进自己的路由表中，并按距离分组存放在 k-buckets 里。渐渐地，我不再依赖 bootnode，而是拥有了属于自己的“联系人网络”。这时候，我已经不再是一个刚进入网络的新节点，而是一个开始理解网络结构的节点。

但即便如此，我仍然没有开始真正的区块或交易同步。因为到目前为止，我只是“认识了别人”，还没有和任何人建立正式、安全的通信。

于是，我进入了下一阶段：RLPx。

如果说 Discovery 是“找人”，那么 RLPx 就是“真正建立关系”。我会从已经认识的节点中挑选一些，尝试和它们建立连接。这一次，我不再使用 UDP，而是使用 TCP，因为我需要的是一个可靠的、持续的通信通道。

在建立连接之前，我会进行一次握手。这一步非常关键。我和对方需要确认彼此的身份，并协商出一套用于这次通信的加密方式。我不会直接使用长期私钥来加密所有通信，而是会生成一对临时密钥。这些临时密钥只用于当前会话，并在会话结束后被丢弃。

这样做的意义在于“前向保密”。即使未来我的长期私钥泄露，过去的通信内容也不会被轻易解密。这让我意识到，节点身份和通信安全是两件不同的事情，必须分别处理。

在握手过程中，我和对方通过 ECDH 等机制计算出一个共享秘密，然后再通过一系列方法派生出真正用于通信的会话密钥。这些密钥会被用于加密消息内容，并通过 MAC 来确保消息没有被篡改。

当握手完成之后，我们之间就建立了一条加密的通信通道。但这还不够。我们还需要确认一件事：接下来我们要用什么协议进行通信。

于是，我发送 Hello 消息，告诉对方我支持哪些 capability，比如 `eth`、`snap` 等。对方也会做同样的事情。只有双方都支持的协议，才会真正被使用。

到这里，我和对方之间才真正建立起一条可用的连接。这条连接不仅是加密的，还支持消息分帧和多路复用。这意味着我可以在同一条连接上，同时运行多个上层协议，而不需要为每个协议单独建立连接。

接下来，真正的核心终于开始了。

我会在这条 RLPx 连接之上运行 `eth` 协议。这个协议负责让我和其他节点交换区块头、区块体、交易和回执。我也可能运行 `snap` 协议，用于高效地同步状态数据。

直到这一刻，我才真正开始参与 Ethereum 执行层网络的“业务”。我可以接收交易、传播交易、同步区块、更新状态。我不再是一个孤立的程序，而是网络中的一个活跃节点。

回头再看整个过程，我才真正理解了 DevP2P 的意义。它不是一个简单的网络协议，而是一整套让节点从“孤立存在”变成“网络成员”的完整路径。

而这条路径，也让我终于把之前学过的所有内容串在了一起。

我通过 JSON-RPC 和节点交互，那是应用层和节点之间的通信；  
节点通过 DevP2P 加入网络，那是节点之间的连接过程；  
节点通过 RLPx 和 `eth` 协议交换数据，那是执行层之间的通信；  
最终，这些数据被执行、被记录，形成状态树和区块结构。

当我把这一切连起来之后，我才真正理解：Ethereum 不只是一个“可以写合约的系统”，它更是一个由无数节点通过复杂但有序的协议协同运行起来的网络。
<!-- DAILY_CHECKIN_2026-04-09_END -->

# 2026-04-08
<!-- DAILY_CHECKIN_2026-04-08_START -->














## 一、 灵魂蓝图：以太坊的设计哲学 (Design Rationale)

以太坊的一切架构决策都源于其核心哲学：**“三明治复杂性模型”**。

-   **协议极简 (Simplicity)：** 底层共识协议必须保持精简。宁愿让开发者在应用层多写代码，也绝不轻易往底层增加复杂逻辑。
    
-   **通用性 (Generality)：** 以太坊不为任何特定应用（如转账、交易所）优化，它只提供一个通用的 **EVM**。在底层眼中，所有的操作都是平等的字节码。
    
-   **非歧视性与中立：** 协议不预设立场，不干预应用逻辑，这种“冷酷”的中立是去中心化信任的基石。
    

* * *

## 二、 性能加速：预编译合约 (Precompiled Contracts)

为了在保持“通用性”的同时解决性能瓶颈，以太坊在 EVM 之外开辟了\*\*“硬件加速通道”\*\*。

-   **本质：** 它们不是用 Solidity 写的，而是直接用 Go/Rust 等原生语言集成在客户端内核中。
    
-   **物理地址：** 占据着 $0x01$ 到 $0x0A$ 的低位地址。
    
-   **作用：** 专门处理极其繁重的数学逻辑（如椭圆曲线加法、哈希运算）。
    
-   **工程逻辑：** 它们是 **“以计算换 Gas”**。通过将高频、昂贵的计算逻辑固化，使得 ZK-Rollups 等先进技术在 L1 上的验证成本变得可以接受。
    

* * *

## 三、 系统骨架：模块化架构 (EL & CL)

自“合并（The Merge）”后，以太坊节点进化为\*\*“双头架构”\*\*，两部分通过 **Engine API** 协同工作。

1.  **执行层 (Execution Layer, EL)：** 负责交易执行、状态管理和账户余额，使用 **MPT (Merkle Patricia Trie)** 结构。它是以太坊的“大脑”。
    
2.  **共识层 (Consensus Layer, CL)：** 负责 PoS 共识、验证者调度和最终确定性检查，使用 **SSZ** 序列化格式。它是以太坊的“脊椎”。
    

* * *

## 四、 共识脉搏：时间、随机性与确定性

以太坊如何确保成千上万个节点在混乱的互联网中保持步调一致？

-   **TTD (终端总难度)：** 在合并期间，以太坊放弃了易被操纵的“区块高度”，选择了**累积计算量 (Total Difficulty)** 作为切换点。这反映了以太坊对物理安全（能量消耗）的尊重。
    
    $$TD\_{n} = TD\_{n-1} + D\_{n}$$
    
-   **RANDAO + VDF (公平随机数)：** RANDAO 通过“众筹种子”产生随机性，而 **VDF (可验证延迟函数)** 则通过强制性的数学计算延迟，封锁了最后揭示者的作弊空间。
    
-   **Slots (12s) 与 Epochs (6.4m)：** 以太坊像节拍器一样运行。Slot 是出块机会，Epoch 是结算周期。通过 **LMD GHOST** 规则，系统能在出现分叉时，根据**质押权重**迅速锁定主链。
    

* * *

## 五、 流水线：区块构建循环 (Block Building Loop)

在执行层内部，区块的产生是一个严密的迭代过程：

1.  **环境初始化：** 接收来自共识层的时间戳、Base Fee 等上下文。
    
2.  **贪婪选择：** 从交易池中按价值（Gas Price）排序拉取交易。
    
3.  **状态转换 (STF)：** 将交易丢进 EVM 运行，更新状态数据库（State DB）。如果交易出错，直接跳过并尝试下一笔，直至 **Gas Limit (3,000万)** 耗尽。
    
4.  **默克尔化：** 计算交易根、收据根和提现根，为区块贴上“防伪封条”。
    

* * *

## 六、 存储革命：从 MPT 到 Verkle Tree

这是以太坊目前最关键的技术跨越。

### 1\. MPT (现行结构) —— “深而重的森林”

-   **结构：** 16 叉树，层级极深。
    
-   **痛点：** 验证一个数据需要提供大量“兄弟节点”的哈希。对于 1000 个数据，见证数据 (Witness) 高达 **4MB**，导致普通设备无法验证。
    

### 2\. Verkle Tree (未来结构) —— “广而轻的平原”

-   **结构：** **256 叉树**。高度大幅降低，31 字节 Stem（树干）+ 1 字节 Suffix（后缀）实现了数据的物理聚集。
    
-   **核心黑科技：向量承诺 (Vector Commitments) & KZG 证明。**
    
    -   它不再使用哈希链，而是使用多项式数学。
        
    -   **聚合证明：** 它可以把整条路径的证明压缩成一个微小的数学点。
        
-   **价值：** 见证数据从 4MB 骤减至 **150KB**。
<!-- DAILY_CHECKIN_2026-04-08_END -->

# 2026-04-07
<!-- DAILY_CHECKIN_2026-04-07_START -->















# 以太坊执行层网络通信学习总结

## —— 从 `eth.md` 理解 EL↔EL、EL↔CL 与现代 Ethereum 节点架构

今天我们学习的核心，是 **Ethereum 执行层节点之间的网络通信协议**，也就是 `devp2p` 仓库里的 `caps/eth.md`。这份文档定义的是 **ETH Wire Protocol**，它运行在 **RLPx** 之上，用来让执行层节点彼此交换区块头、区块体、交易池交易、回执等执行层数据。文档本身直接说明，`eth` 协议是用于 peers 之间交换 Ethereum blockchain information 的协议，并且当前基线版本写的是 `eth/69`。

很多人在第一次接触这份文档时，最容易误解的一点是：以为它讲的是“智能合约怎么执行”或者“DApp 怎么和链交互”。其实不是。今天我们已经把这层关系理清了：**前端、钱包、后端程序** 通常是通过 **JSON-RPC** 和节点交互；**共识层 CL 和执行层 EL** 主要通过 **Engine API** 交互；而 `eth.md` 讲的是 **执行层节点和执行层节点之间** 如何互相同步和传播执行层数据。换句话说，它不属于合约开发接口，而属于节点网络层接口。ethereum.org 对节点的说明也印证了这一点：执行层客户端负责监听网络中广播的新交易、在 EVM 中执行它们，并保存最新状态与数据库；共识层客户端负责 PoS 共识，使网络就经过验证的数据达成一致。

* * *

## 一、为什么今天的 Ethereum 不能只有 EL↔CL，还必须有 EL↔EL

这是今天最重要的理解突破之一。你提出的问题非常关键：**“既然 EL 已经和 CL 对接了，为什么 EL 还要和别的 EL 通信？”** 这个问题一旦想通，整个 `eth.md` 的定位就会变得非常清楚。

答案是：**EL↔CL 和 EL↔EL 解决的是两类完全不同的问题。**  
EL↔CL 解决的是“共识层和执行层之间如何协作”；EL↔EL 解决的是“执行层数据如何在整个执行层网络中传播和同步”。Engine API 的 Paris 规范显示，CL 和 EL 的交互核心围绕 `engine_newPayloadV1`、`engine_forkchoiceUpdatedV1`、`engine_getPayloadV1` 等方法展开，其中 `ForkchoiceState` 里有 `headBlockHash`、`safeBlockHash`、`finalizedBlockHash`，而 `ExecutionPayload` 则包含执行层区块内容，例如 `transactions`。这说明 CL 主要是在告诉 EL：当前哪条链头是该接受的，你该围绕哪个 head 更新 forkchoice，或者基于哪个 head 构建新的 payload。

但这还远远不够。因为就算 CL 告诉了 EL “哪个 head 是正统链头”，EL 仍然要自己解决很多现实问题：**交易从哪里来，区块体从哪里下载，回执从哪里补，历史数据从哪里拿，本地交易池如何传播给更多 peer。** 这些都不是 CL 替它做的，而是执行层网络自己完成的。`eth.md` 明确把一个 session 中的高层任务分成两类：**chain synchronization** 和 **transaction exchange**。这就意味着，EL 节点之间的横向通信不是可选装饰，而是执行层能形成一个网络、能让数据流动起来的基础。

所以今天最值得记住的一句话是：

**CL 决定“该信哪条链”，EL 网络负责“把这条链需要的执行层数据搬过来并传播开”。**  
没有 EL↔CL，执行层不知道该跟随哪条 canonical chain；没有 EL↔EL，执行层就没有足够的数据来源和传播网络，整个系统也跑不起来。

* * *

## 二、`eth.md` 的本质：它讲的是执行层节点之间的“数据交换规则书”

从整体上看，`eth.md` 不是在讲“链为什么存在”，也不是在讲“EVM 指令如何执行”，而是在讲一个更基础的问题：**两个执行层节点一旦连上了，应该如何互相说话。**

这份文档规定了哪些消息可以发、先发什么、后发什么、每种消息结构长什么样、响应应该怎么返回。它相当于执行层节点之间的一本“通信规则书”。文档在 Basic Operation 里明确写到：连接建立后，双方必须先交换 `Status` 消息；只有在收到对方 `Status` 之后，这个 Ethereum session 才算激活，在此之前不应该发送其他 `eth` 消息。文档还规定了消息尺寸限制，底层 RLPx 单条消息上限为 16.7 MiB，而 `eth` 协议实践中常见上限更低，典型实现会将单消息限制在约 10 MiB 左右。

这一部分你可以用一句非常朴素的话来理解：  
`eth` **协议不是“随便发消息”，而是“先握手，再按规则同步数据”。**  
这和日常应用层 API 的感觉很不一样，它更接近底层网络协议的思路。

* * *

## 三、Status 握手：不是简单“打招呼”，而是能力与状态通告

今天我重点看了 `Status` 这个消息，因为它是整个 `eth` session 的起点。根据 `eth.md`，`Status` 的格式是：

`[vsn, networkid, genesis, forkid, earliest, latest, latestHash]`

这里面包含了协议版本、网络 ID、创世块哈希、fork 标识，以及当前节点能提供的完整区块历史范围。也就是说，`Status` 并不只是“你好，我在线”，而是“我运行的是哪个协议版本、我在哪条网络、我处于哪个 fork 规则、我现在到底能服务哪一段历史”。

这里还有两个今天特别容易混淆、但我们已经厘清的知识点。

第一，`networkid` **不一定等于** `chainId`。`networkid` 更偏向 P2P 网络层的识别；`chainId` 则更多用于交易签名与重放保护。文档明确提醒，这两者不应想当然地视为同一个值。

第二，`Status` 在 `eth/69` 之后比以前更重要。EIP-7642 明确指出，`eth/69` 做了三件关键事：  
一是让节点在握手里声明自己服务的历史区块范围；  
二是移除了 Merge 后已不再使用的 total difficulty 握手信息；  
三是简化了 receipts 传输格式。

因此，今天你再看 `Status`，不能只把它当成旧时代的“版本对齐”，而应理解为：**现代 Ethereum 执行层节点用它来告知 peer 自己的历史服务能力和当前兼容上下文。**

* * *

## 四、为什么 PoS 之后，`eth.md` 不能再按 PoW 思路去读

这是今天学到的另一个非常关键的点。`eth.md` 本身明确写到：因为 Ethereum 的共识发生在 execution chain 之外，这个协议没有内建机制来决定 canonical chain head，它假定每个节点会通过其他方式，比如和一个 consensus node 通信，来知道 canonical chain。

这意味着，**Merge 之后，**`eth` **协议不再单独承担“哪条链算正统”这件事。**  
在 PoW 时代，执行层网络自己就更接近“既传播数据，也决定哪条链更重”；但在 PoS 时代，canonical head、safe head、finalized head 的判断，已经主要由共识层提供。Engine API 里的 `ForkchoiceState` 正是这种变化的体现，因为它直接让 CL 向 EL 传达 head、safe、finalized 三种哈希。

文档还特别提醒，PoW 转 PoS 之后，**block propagation 已不再由** `eth` **协议负责**；文中保留的 `NewBlock`、`NewBlockHashes` 相关描述，主要只适用于 PoW 和部分 PoA 网络，并会在未来版本中逐步淡出。也就是说，今天阅读 `eth.md` 时，要把它看成一份“带有历史包袱但主线已经现代化”的协议文档：有些内容仍在规范里，但它们对当前主网的核心理解权重要降低。

* * *

## 五、链同步主线：先拿 headers，再拿 bodies，再补 receipts

在今天的学习中，我们把 `eth.md` 里最经典的一条主线理清了：**chain synchronization**。

文档给出的典型流程是这样的：节点先通过 `GetBlockHeaders` 请求区块头，对方返回 `BlockHeaders`；在拿到 header 链之后，再用 `GetBlockBodies` 请求区块体，由对方返回 `BlockBodies`；在某些同步场景下，还要额外用 `GetReceipts` 拉取 receipts，由 `Receipts` 响应。`GetBlockHeaders` 的请求参数中还包含 `startblock`、`limit`、`skip`、`reverse`，这意味着节点不只是“从头到尾一块一块拉”，还可以先取骨架，再并行补全缺失部分。

今天我们特别强调了：  
**为什么一定是先拿 header，再拿 body？**  
因为 header 更轻，它决定了“区块骨架”和各个根承诺；一旦 header 链确定下来，节点就知道每个 block hash 是什么，于是区块体可以向多个 peer 并行请求。之后 receipts 又可以用来校验 `receipts-root`，从而让同步过程不必每次都立刻完整重放所有交易。

这一条流程线，实际上是理解执行层节点“为什么能同步起来”的关键。如果没有 EL↔EL 网络，这条链同步路径本身就不存在，因为 headers、bodies、receipts 都是从其他执行层 peer 拉来的。

* * *

## 六、State sync 为什么不再属于 `eth` 主协议

今天我还学到一个很容易被忽略，但实际上非常体现现代架构变化的点：**state tree 下载已经不再是** `eth` **协议的工作。**

`eth.md` 明确说明，`eth/63` 到 `eth/66` 还可以通过 `eth` 协议同步 state tree，但从 `eth/67` 开始，这项工作已经被交给了辅助协议 `snap`。官方 `snap.md` 也说明，`snap` 运行在 RLPx 之上，用于 peers 之间交换 Ethereum 状态快照。

这意味着今天你理解执行层同步时，不能再想成“一个 `eth` 协议包办一切”。更准确地说：

-   `eth` 主要负责 **headers、bodies、receipts、交易池传播**；
    
-   `snap` 负责 **state 下载**；
    
-   `Engine API` 负责 **EL 和 CL 之间的共识-执行接口**。
    

这恰恰体现了现代 Ethereum 架构正在不断模块化、分层化：不同协议负责不同职责，而不是所有东西挤在一个协议里。

* * *

## 七、交易传播：为什么交易池一定要靠 EL 网络自己扩散

交易交换是今天学习里非常重要的一部分，因为它和你以后理解 mempool、机器人、脚本并发冲突都直接相关。

ethereum.org 说明，执行层客户端会监听网络中广播的新交易并执行它们。`eth.md` 进一步规定，节点之间必须交换 pending transactions，并把它们保存在本地的 transaction pool 中。新 peer 建立连接时，会先用 `NewPooledTransactionHashes` 公告自己本地池中的交易哈希；如果对方发现有自己没有的交易，就会用 `GetPooledTransactions` 拉取完整交易；此外还有 `Transactions` 消息，可直接发送完整交易。

这说明交易传播的核心逻辑是：

**先广播目录，再按需拉正文。**

这么做的好处是节省带宽。不是每个节点都必须把每一笔完整交易无差别地发给所有 peer，而是先告诉别人“我这边有这些交易的哈希、类型、大小”，真正需要的人再来要正文。文档还特别说，节点应该避免把已经知道的交易重复回发给同一个 peer。

这一部分也直接回答了你那个问题：  
**为什么不能只有 EL↔CL？**  
因为交易池传播本来就是 EL 网络内部的职责。CL 负责共识，不负责充当执行层交易广播总线。

* * *

## 八、交易有效，不等于最终执行成功

今天我们还补上了一个非常重要、以后你写 Web3 脚本时一定会反复遇到的概念：**mempool valid 不等于 EVM execute success。**

`eth.md` 在“Transaction Encoding and Validity”一节明确指出，协议所说的交易有效性，关注的是这笔交易是否适合临时放入本地池并与其他 peer 交换，而不是它最终一定能在 EVM 中成功执行。文档要求节点检查交易类型是否已知、签名是否有效、gas 是否满足 intrinsic gas、余额是否足够支付 value 和 gas 成本，还要求交易 nonce 至少不小于发送者当前账户 nonce。

这意味着，一笔交易即使已经进入 mempool，也仍然可能在真正打包执行时失败。失败原因可能是状态变化、滑点变化、余额被其他交易先消耗、或者同 nonce 交易竞争替换。今天虽然没有把这些交易池实现细节展开成一门课，但你已经抓住了最底层的逻辑：  
**网络层接受一笔交易，并不等于链上最终一定成功执行这笔交易。**  
这是理解 Web3 交易行为、尤其是 DeFi 交互和自动化机器人时非常关键的一个思维转折。

* * *

## 九、今天关于 nonce 的真正上下文：交易 nonce 和区块 nonce 不一样

今天专门到了 nonce，这个问题很重要，因为 `eth.md` 里其实出现了两个完全不同的 nonce。

第一种是 **transaction nonce**。它出现在 legacy transaction 的字段里，表示发送者账户的交易序号。文档在交易有效性检查里要求节点确认该 nonce 至少不小于当前账户 nonce，也允许实现自行决定本地池支持多大的 future transaction gap。这个 nonce 主要和账户级交易顺序、重放保护、交易池排队有关。

第二种是 **block nonce**。它出现在 block header 结构中，是历史 PoW 区块头字段的一部分，和 seal 校验有关。文档在 header 编码与 header validity 部分都保留了这个字段，还提到可检查的 seal 包括 `mix-digest` 和 `block-nonce`。

所以今天一个非常重要的收获是：  
**你以后看到“nonce”这个词，绝不能下意识只想到“账户交易序号”。**  
在 Ethereum 协议文档里，它可能是 tx nonce，也可能是 block nonce，必须看上下文。今天在 `eth.md` 里，这两个都出现了。

* * *

## 十、区块结构：为什么一个区块不是“只有交易列表”那么简单

今天我们还系统看了区块结构。`eth.md` 给出的 header 字段远比初学者想象得复杂：有 `parent-hash`、`state-root`、`txs-root`、`receipts-root`、`gas-limit`、`gas-used`、`basefee-per-gas`、`withdrawals-root`、`blob-gas-used`、`excess-blob-gas`、`parent-beacon-root`、`requests-hash` 等。它还定义了 block body 为 `[transactions, ommers, withdrawals]`。

今天最值得记住的，是对“header、body、block”这三者关系的理解：

-   **header** 是摘要和承诺，里面有各种 root 和关键元数据；
    
-   **body** 是正文，真正放交易、ommers、withdrawals 等；
    
-   **完整区块** 则是 header 加上相应正文内容。
    

也正因为 header 里有 `txs-root`、`receipts-root`、`state-root` 等承诺字段，所以同步流程才能先拿 header，再用 body 和 receipts 去校验这些根是否一致。

* * *

## 十一、为什么 header 字段会越来越多：因为 Ethereum 协议还在持续演进

今天我特别提到，`eth.md` 不是一份“死掉的旧规范”，而是一份随硬分叉不断更新的活文档。文档在 header validity 规则里明确写出了不同 fork 之后哪些字段必须存在、之前则必须不存在。比如：

London 之后必须有 `basefee-per-gas`；  
Shanghai 之后必须有 `withdrawals-root`；  
Cancun 之后必须有 `blob-gas-used`、`excess-blob-gas`、`parent-beacon-root`；  
Prague 之后必须有 `requests-hash`。

这让你今天对 Ethereum 的认识更进一步了：  
**Ethereum 不是一个固定不变的“老协议”，而是在持续迭代的系统。**  
每次升级不仅会影响合约开发层，也会改变执行层 header、区块编码、节点网络协议等底层结构。`eth.md` 正是这些演进在执行层 P2P 规范上的反映。

* * *

## 十二、eth/69 的现代重点：历史范围通告与 receipts 简化

今天我们花了不少时间理解 `eth/69` 为什么重要。EIP-7642 给出的核心动机很明确：随着 Ethereum 进入 **history expiry** 时代，并不是所有节点都会永久保存并提供全部历史。因此，协议需要一种机制让节点告诉 peer：**我到底还能服务哪一段历史。** 这就是为什么 `Status` 里新增了 `earliest`、`latest`、`latestHash`，并新增了 `BlockRangeUpdate` 消息。

`BlockRangeUpdate` 的作用，就是在节点历史服务范围变化时，通知 peer 新的 `[earliest, latest, latestHash]`。文档还建议，这种更新没必要每个新区块都发一次，大约每两分钟发一次就可以。

EIP-7642 还指出一个非常现实的优化点：旧版 receipts 传输里带着 Bloom 字段，但客户端实际上通常不会持久化这个字段，因为它可以重新计算。继续在网络上传这个字段会浪费巨量带宽，EIP-7642 估计仅此一项在同步中就会造成大约 **530GB** 的额外传输。因此 `eth/69` 简化了 receipts 编码，移除了 Bloom，并简化了 typed receipts 的表示。

这一部分的本质，是让你看到现代 Ethereum 协议设计正在越来越现实化：  
**不是默认每个节点都全量保留、全量传输、全量服务，而是承认资源边界，并优化真正有价值的数据交换。**

* * *

## 十三、eth/70 草案：为什么 receipts 还要继续分页化

今天我还顺带接触到了 `eth/70` 的方向。`eth.md` 的 changelog 里已经写到了 `eth/70` 的相关更新，而 EIP-7975 则说明了它的目标：支持 **partial block receipt lists**，也就是对超大区块的 receipts 列表进行分页获取。原因是随着主网 block gas limit 提高，单个区块的 receipts 列表未来可能超过客户端常用的约 10 MiB 消息大小限制。需要注意的是，EIP-7975 目前仍是 **Draft**。

这一点虽然还没有完全成为稳定基线，但它很好地说明了协议演进方向：  
**Ethereum 执行层网络协议正在从“大包整传”逐步走向“更细粒度、更可分页、更适应大规模链上数据”的模式。**

* * *

## 十四、今天真正建立起来的整体架构图

如果把今天的学习内容压缩成一张“脑内结构图”，它大概是这样的：

用户、前端、钱包、后端  
通过 **JSON-RPC** 和执行层节点交互；  
执行层节点和共识层节点  
通过 **Engine API** 协作；  
执行层节点彼此之间  
通过 **ETH Wire Protocol (**`eth`**)** 交换区块头、区块体、回执和交易池数据；  
执行层节点如需状态快照  
通过 **SNAP** 协议同步状态；  
共识层节点彼此之间  
则运行各自的共识层网络协议。

今天最大的进步，不只是学会了几个新名词，而是开始真正把这些层次分开看了。以前容易把“链”“节点”“客户端”“RPC”“共识”“执行”“同步”都揉成一团；今天开始，这些概念已经被拆成了不同角色、不同通道、不同职责。
<!-- DAILY_CHECKIN_2026-04-07_END -->

# 2026-04-06
<!-- DAILY_CHECKIN_2026-04-06_START -->
















# 我对以太坊协议层的一次系统理解：从 EL、CL 到区块确认与 Engine API

最近我系统学习了以太坊协议层，最大的变化是：我不再只把以太坊看成“一个能跑智能合约的链”，而开始把它理解成一个**分层协作的系统**。

在最开始接触以太坊时，我更关注的是合约怎么写、交易怎么执行、状态怎么变化。但随着我继续往下学，我发现如果只停留在“智能合约执行”这一层，其实很难真正理解以太坊节点到底是怎么工作的。因为今天的以太坊，已经不是一个单体程序就能解释清楚的系统了。The Merge 之后，一个完整的以太坊节点不再只是一个客户端，而是至少由\*\*执行客户端（EL）**和**共识客户端（CL）\*\*两部分组成；如果这个节点还要参与出块和投票，还需要运行验证者客户端。也就是说，现在的“节点”本质上是多个角色协同工作的结果。

在我的理解里，EL 和 CL 的分工非常明确。EL 负责执行交易、运行 EVM、维护账户余额和合约状态，也负责对外提供常见的 JSON-RPC 接口；CL 负责 PoS 共识，接收和传播区块与 attestation，根据共识规则决定当前应该跟随哪条链。简单说，EL 更像“执行者”，CL 更像“裁判和协调者”。以前我总容易把“执行”和“共识”混在一起，现在我会明确区分：**执行层负责状态转移，共识层负责链选择。**

我后来还搞明白了一件很关键的事：EL 和 CL 虽然属于同一个节点，但它们**并不运行在同一套 P2P 网络上**。执行层有自己的网络，主要传播交易；共识层有自己的网络，主要传播区块和 attestation。两者之间不是直接混在一起工作的，而是通过专门的接口协作，这套接口就是 **Engine API**。所以现在我理解的以太坊，不是一条单线网络，而是“执行网络 + 共识网络 + 本地协作接口”组成的系统。

继续往下学之后，我开始真正理解 PoS 下“共识”到底是什么意思。现在的以太坊不再依赖矿工，而是依赖验证者。每个 slot 会随机选出一个区块提议者来提议新区块，同时会有一组验证者对区块进行 attestation。attestation 本质上就是投票：验证者会对自己当前看到的链视图进行签名并广播。共识客户端会不断收集这些投票，然后运行 fork choice，选择当前权重最大的那条链作为链头。也就是说，CL 不是随便决定哪条链是对的，而是根据验证者的投票结果动态选出当前应该跟随的 head。

这一步让我终于明白了一个以前很困惑的问题：**为什么 head 会不稳定。** 一开始我会觉得，既然最后总是要统一，那为什么不能一开始就所有人同时确认？后来我才真正意识到，问题不在于“大家不想统一”，而在于分布式网络里，所有节点不可能在同一瞬间看到完全相同的信息。有的节点先收到了新区块，有的节点还没收到；有的节点先看到了更多 attestation，有的节点稍慢一点。于是，在某个很短的时间窗口里，不同节点可能会基于各自手头的信息，得出不同的“当前最新链头”判断。这不是系统出错，而是分布式系统里非常正常的网络传播延迟现象。

我现在会这样理解 `head`：它表示**当前视图下最新、权重最高的链头**，但它不是最终确认。它之所以会变化，是因为 fork choice 会随着更多 attestation 的到来而动态调整。也就是说，`head` 本质上强调的是“最新”，不是“最终”。这一点特别重要，因为它解释了为什么区块链不能只提供一种确认状态。

接下来我学习到的另一个重点，就是以太坊为什么要区分多种确认层级。现在我会把它们理解成不同强度的确认状态：`head` 代表最新链头，速度最快，但也最容易变化；`safe` 代表更稳妥的安全头；`justified` 是共识层里 checkpoint 已经获得强支持的状态；`finalized` 则是最终确认状态，基本可以认为不会再被回滚。以太坊之所以要分出这些层级，不是因为设计重复了，而是因为不同层级对应着不同程度的确定性。

我后来也想通了：**为什么不能只保留一个最高等级的确认，比如 finalized。** 原因很简单，因为越强的确认通常意味着越慢。如果所有业务都必须等到 finalized 才能继续，那很多需要实时反馈的场景都会变得非常迟钝。比如钱包余额展示、区块浏览器追踪最新区块，这些都更需要 `latest/head`；而跨链桥、大额结算、清算这类更重视安全的场景，就更适合看 `safe` 甚至 `finalized`。所以在我看来，这不是协议“设计得太复杂”，而是它在认真处理一个现实问题：**不同业务对速度和安全的要求并不一样。**

如果用最通俗的话来说，我现在会把这几个确认层级理解成这样：`head` 是“刚刚上链、最新看到的结果”，`safe` 是“已经比较稳了”，`justified` 是“已经获得很强的集体背书”，而 `finalized` 是“最终确认，基本不会再回头”。这样理解以后，我再看这些术语，就不会觉得它们只是一些抽象名词，而是能把它们放到真实的链上确认流程里去理解。

在理解确认层级之后，我又进一步接触到了 EL 和 CL 之间最关键的接口，也就是 **Engine API**。这里我最重要的收获，是把它和普通 JSON-RPC 彻底区分开。普通 JSON-RPC 主要是给钱包、前端、脚本和开发框架使用的，而 Engine API 是给执行层和共识层“内部协作”使用的。它不是给普通 dApp 前端直接调的，而是 CL 和 EL 在同一个节点内部交换链头信息、payload 和兼容性信息的方式。这个区分对我特别重要，因为以前我总觉得“反正都是 RPC”，现在我知道：**外部应用和 EL 说话，CL 和 EL 也在说话，但这是两种完全不同的接口语境。**

在 Engine API 里，我目前最关注的几个方法是 `engine_exchangeCapabilities`、`engine_forkchoiceUpdated`、`engine_getPayload` 和 `engine_newPayload`。一开始这些名字看起来都很像，我也很容易混，但现在我已经能把它们分开理解了。

`engine_exchangeCapabilities` 并不是传区块，也不是执行共识，它只是让 EL 和 CL 先互相告诉对方：**我当前支持哪些 Engine API 方法版本。** 它解决的是接口兼容性问题，而不是链上状态问题。换句话说，它像是一种“先报家底”的过程：在双方正式协作之前，先互相确认“我会哪些方法、你会哪些方法、我们接下来能用哪些版本的接口配合”。我现在会把它理解成一种很轻量、但很有工程意义的能力协商机制。

为什么这一步重要？因为 Engine API 本身不是永远不变的。随着协议演进、硬分叉推进，方法版本可能会升级，旧方法可能会废弃，新方法也可能会加入。如果没有 `engine_exchangeCapabilities` 这样的机制，那么 EL 和 CL 即使各自都正常运行，也可能因为接口版本不一致而合作失败。所以这个方法虽然不直接处理区块和状态，但它其实是在为后续真正的协作打基础。

真正把“共识层当前认哪条链”告诉执行层的核心方法，是 `engine_forkchoiceUpdated`。我现在会把它理解成：CL 先通过验证者投票和 fork choice 算法选出当前应该跟随的链头，然后把这个结果通过 `headBlockHash`、`safeBlockHash` 和 `finalizedBlockHash` 传给 EL。也就是说，EL 并不会自己决定现在该跟哪条链，而是接受 CL 已经计算好的链视图。

更进一步，如果这个调用里还附带了 `payloadAttributes`，那它不仅是在更新链头视图，还相当于在对 EL 说：**“请基于这个 head，开始帮我构建一个新的 execution payload。”** 这一步非常关键，因为它把“共识层选链”和“执行层构建区块执行内容”衔接了起来。也正是从这里开始，我真正理解了 forkchoice 和 block building 并不是两件独立的事，而是同一条工作流里的前后两步。

这时候，`engine_getPayload` 和 `engine_newPayload` 的区别就清楚了。`engine_getPayload` 是把 EL 本地已经构建好的 execution payload 取出来给 CL；而 `engine_newPayload` 是把一个 execution payload 交给 EL，请它按执行规则去验证这个 payload 是否有效。前者更像“把你刚造好的块拿给我”，后者更像“这里有一个块，请你检查它是不是合法的”。

我之前最容易卡住的一点是：EL 又构建、EL 又验证，那不是永远都说自己对吗？后来我才真正想明白，不是“某一个 EL 说自己对就算对”，而是**每个节点的 EL 都会独立重放和验证 payload**。提议者本地 EL 可以先构建候选 payload，但网络里的其他节点收到之后，也会把这个 payload 交给各自的 EL 重新执行。所以最终能不能被全网接受，并不是某一个 EL 说了算，而是全网节点一起独立检查之后的结果。也正因为这样，我才真正理解了为什么执行层的“验证”不是自说自话，而是整个网络一致性的一部分。

把这些内容全部串起来之后，我现在会这样理解以太坊的这套架构：**以太坊节点是 EL 和 CL 的组合；EL 负责执行交易和维护状态，CL 负责共识和链头选择；两者跑在不同的 P2P 网络上，同时通过 Engine API 交换链头信息、payload 和能力信息。**`engine_exchangeCapabilities` **负责先协商接口能力，**`engine_forkchoiceUpdated` **负责把当前链头视图传给 EL，**`engine_getPayload` **负责取回 EL 构建好的 execution payload，**`engine_newPayload` **则负责让 EL 对 payload 进行验证。**

这样一来，我再去看 STF、forkchoice、payload、head、safe、justified、finalized 这些概念时，就不会把它们当成散乱的术语，而会知道它们其实是在共同描述一个完整闭环：**共识层先决定当前链视图，执行层在这个视图下执行和构建，网络中的其他节点再独立验证，最后区块从最新逐步走向最终确认。**

如果让我用一句话概括我这部分学习最大的收获，我会这样说：

**以太坊不是“一个程序执行交易”那么简单，而是一个分层协作的系统。CL 通过 PoS 和验证者投票决定当前链视图，EL 在这个视图下执行和验证状态变化，两者通过 Engine API 协作；而区块从最新到最终确认，也不是一步完成，而是经历从 head 到 safe，再到 justified 和 finalized 的逐步增强过程。**

当我真正把这些东西连起来之后，再回头看交易确认、合约执行、节点架构和 RPC 接口，很多原本零散的概念就终于串成了一条完整的线。
<!-- DAILY_CHECKIN_2026-04-06_END -->
<!-- Content_END -->
